<nav class="navbar navbar-expand-lg navbar-dark bg-blue fixed-top">
    <div class="container">
        <div class="logo">

            <a class="navbar-brand" href="/"><img src="/assets/images/servyyLogo.png" alt="Servyy"></a>
            <div class="slogan">
                <h3>Servyy</h3>
                <p>Les services qui vous changent la vie</p>
            </div>
        </div>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarResponsive"
                aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarResponsive">
            <ul class="navbar-nav ml-auto">
                {% if session %}
                    {% if session.status == 1 %}
                        <li class="nav-item{% if server.REQUEST_URI == '/Admin/index' %} active{% endif %}">
                            <a class="nav-link" href="/Admin/index">Espace Admin
                                <span class="sr-only">(current)</span>
                            </a>
                        </li>
                    {% endif %}

                    {% if session.status == 0 %}
                        <li class="nav-item{% if server.REQUEST_URI == '/Member/index' %} active{% endif %}">
                            <a class="nav-link" href="/Member/index">Espace Membre
                                <span class="sr-only">(current)</span>
                            </a>
                        </li>
                    {% endif %}
                {% endif %}
                <li class="nav-item{% if server.REQUEST_URI == '/Service/index' %} active{% endif %}">
                    <a class="nav-link" href="/Service/index">Services
                        <span class="sr-only">(current)</span>
                    </a>
                </li>
                <li class="nav-item{% if server.REQUEST_URI == '/Contact/index' %} active{% endif %} ">
                    <a class="nav-link " href="/Contact/index">Contact</a>
                </li>


                {% if session == false %}
                <li class="nav-item {% if server.REQUEST_URI == 'Inscription/register' %} active{% endif %}">
                    <a class="nav-link" href="/Inscription/register">S'inscrire</a>
                </li>
                {% endif %}
                {% if session.email %}
                    <li class="nav-item">
                        <a class="nav-link" href="/Login/logout">Se Deconnecter</a>
                    </li>
                {% endif %}
                {% if session == false %}
                    <li class="nav-item {% if server.REQUEST_URI == '/Login/index' %}} active{% endif %}">
                        <a class="nav-link" href="/Login/index">Se Connecter</a>
                    </li>
                {% endif %}


            </ul>
        </div>
    </div>
</nav>